{
  "name": "@video-to-doc/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": { "@video-to-doc/contracts": "workspace:*" },
  "devDependencies": { "vitest": "^3.2.0" }
}
